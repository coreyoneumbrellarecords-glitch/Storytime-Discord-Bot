# Required runtime secrets. Add these through Replit Secrets rather than committing them.
DISCORD_TOKEN=
OPENROUTER_API_KEY=

# Optional settings
OPENROUTER_MODEL=openrouter/free
DB_NAME=storytime_bot.db